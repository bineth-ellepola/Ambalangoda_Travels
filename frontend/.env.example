# Optional: override the backend API base URL.
# Defaults: "/api" in development (proxied to the local backend),
# https://ambalangoda-travels.onrender.com/api in production builds.
# VITE_API_URL=https://ambalangoda-travels.onrender.com/api
